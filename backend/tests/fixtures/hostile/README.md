# Hostile fixtures

Recorded/authored transcripts where the counterparty misbehaves. Add one whenever
someone finds a new way to break the agent by voice — the fixture is the bug report.

One file per scenario, named to match `scripts/sim_call --scenario <name>`.
Transcripts only. Never commit audio (`*.wav`, `*.mp3` are gitignored).
